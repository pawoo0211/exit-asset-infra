apiVersion: v2
name: airflow-local
description: Wrapper chart for Apache Airflow local deployment
type: application
version: 0.1.1
appVersion: "2.10.5"
dependencies:
  - name: airflow
    version: "1.16.0"
    repository: "https://airflow.apache.org"

apiVersion: v2
name: prometheus-local
description: Wrapper chart for Prometheus local deployment
type: application
version: 0.1.0
appVersion: "2.55.0"
dependencies:
  - name: prometheus
    version: ">=27.0.0 <28.0.0"
    repository: "https://prometheus-community.github.io/helm-charts"
